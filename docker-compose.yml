version: '3.8'
services:
  mysql:
    container_name: 'shorten_it_mysql'
    image: 'mysql:8.1'
    environment:
      - MYSQL_DATABASE=shortendb
      - MYSQL_PASSWORD= docker
      - MYSQL_ROOT_PASSWORD=docker
      - MYSQL_USER=docker
    ports:
      - '3306:3306'